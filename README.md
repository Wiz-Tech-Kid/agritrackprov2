
  # AgriTrack Prov2

  

  ## Running the code

  Run `pnpm install` to install the dependencies.

  Run `pnpm start` to start the development server on `192.168.1.104:5000`.

  Run `pnpm run build` to create a production build in `dist/`.

  ## Netlify

  This project includes `netlify.toml` with:

  - Build command: `pnpm run build`
  - Publish directory: `dist`

  For manual Netlify uploads, upload the generated `dist/` folder after running `pnpm run build`.
  
